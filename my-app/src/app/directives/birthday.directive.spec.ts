import { BirthdayDirective } from './birthday.directive';

describe('BirthdayDirective', () => {
  it('should create an instance', () => {
    const directive = new BirthdayDirective();
    expect(directive).toBeTruthy();
  });
});
